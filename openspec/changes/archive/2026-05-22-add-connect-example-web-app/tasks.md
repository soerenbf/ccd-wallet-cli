## 1. Example app package setup

- [x] 1.1 Add a new example web application package under `packages/`.
- [x] 1.2 Configure Vite, React, and TypeScript for the example package.
- [x] 1.3 Wire the example package to depend on `@ccd-wallet/connect-client` from the workspace.

## 2. Integration reference implementation

- [x] 2.1 Implement a simple React UI for connect-server URL entry and six-digit challenge generation/regeneration.
- [x] 2.2 Implement the pairing flow through the TypeScript connect client package.
- [x] 2.3 Display session token, network genesis hash, and account address after successful pairing.
- [x] 2.4 Implement session-context refresh and local reset behavior.

## 3. Validation and documentation

- [x] 3.1 Add validation/tests covering the example app's core integration flow.
- [x] 3.2 Document how to run the example app and explain that it is an integration reference rather than a production-ready wallet UI.
